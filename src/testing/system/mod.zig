//! System tests - full application running end-to-end.
//!
//! These tests exercise the entire stack: domain + presentation + rendering.
//! Run with: zig build test-system

const std = @import("std");

// TODO: System tests will be added when needed.
// Unlike integration tests, these run the full app loop.

test "placeholder: system tests not yet implemented" {
    // Future: headless app tests, replay tests, etc.
}
